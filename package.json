{
  "name": "tbello",
  "version": "1.0.0",
  "description": "Tbellos Github Profile",
  "main": "src/app.ts",
  "scripts": {
    "build": "yarn run tsc",
    "build:watch": "yarn run tsc-watch",
    "start": "yarn build && node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "markdown-it": "^14.0.0",
    "rss-parser": "^3.13.0",
    "tsc-watch": "^6.0.4",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.4"
  }
}
